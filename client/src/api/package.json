{
	"name": "server",
	"version": "1.0.0",
	"description": "servidor que realiza la limpieza total del carrito",
	"main": "index.js",
	"scripts": {
		"start": "nodemon index.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "ignacio morales",
	"license": "ISC",
	"dependencies": {
		"cors": "^2.8.5",
		"express": "^4.19.2",
		"fs": "^0.0.1-security"
	},
	"devDependencies": {
		"nodemon": "^2.0.22"
	}
}
