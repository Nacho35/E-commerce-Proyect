{
	"version": 2,
	"builds": [
		{ "src": "client/**", "use": "@vercel/static" },
		{ "src": "api/**", "use": "@vercel/node" }
	],
	"routes": [
		{ "src": "/api/([^/]*)", "dest": "api/$1" },
		{ "src": "/(.*)", "dest": "client/public/$1" }
	]
}
